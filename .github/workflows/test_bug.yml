name: release

on:
  push:
    branches: ["fix/build_without_c_lib"]

jobs:
#   test-build:
#     strategy:
#       matrix:
#         platform: [ubuntu-latest]
#         go-version: ["1.20"]
#     name: Release
#     runs-on: ${{ matrix.platform }}
#     steps:
#       - name: Setup Go
#         uses: actions/setup-go@v4
#         with:
#           go-version: ${{ matrix.go-version }}

#       - name: Checkout
#         uses: actions/checkout@v3
#         with:
#           fetch-depth: 0

#       - name: Set Node.js 19.x
#         uses: actions/setup-node@v3
#         env:
#           NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
#         with:
#           node-version: 19.x
#           registry-url: "https://registry.npmjs.org"

#       - name: Run install
#         uses: borales/actions-yarn@v4
#         with:
#           cmd: install --ignore-optional

#       - name: Build
#         run: |
#           yarn run build:client
#           cp -r dist backend/dist
#           cd backend
#           bash build.sh
  test:
    strategy:
      matrix:
        platform: [ubuntu-18.04]
    name: Release
    runs-on: ${{ matrix.platform }}
    steps:
      - name: Run
        run: |
           wget https://github.com/HuolalaTech/page-spy-web/releases/download/v1.2.2/page-spy-api-linux-arm64.tar.gz
           tar -xzf page-spy-api-linux-arm64.tar.gz
           ./page-spy-api

